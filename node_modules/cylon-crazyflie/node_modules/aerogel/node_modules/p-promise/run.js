var P = require ('./p');

function addOne( x ) {
	return x + 1;
}

for ( var i = 0; i < 10000; i++) {
	var d = P.defer();
	d.promise.then(addOne)
	var p = d.promise.then(addOne).then(addOne);
	d.resolve(1);
}

// node --trace_opt --trace_deopt run.js
